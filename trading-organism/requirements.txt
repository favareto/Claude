# Darwin Agent v2.3 Dependencies
aiohttp>=3.9.0          # Async HTTP for market APIs + web dashboard
pyyaml>=6.0             # Configuration files
numpy>=1.24.0           # ML brain computations + feature engineering
anthropic>=0.40.0       # Investigador (investigator_research.py) — pesquisa via API + busca web
yfinance>=0.2.40        # markets/yahoo.py — preços de ações/índices/commodities/futuros (grátis, sem chave)
pandas>=2.0.0           # yfinance depende disso; também usado pra reamostrar 1h -> 4h em markets/yahoo.py
